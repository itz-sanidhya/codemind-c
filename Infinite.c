#include<stdio.h>
int main()
{
    int a,b,c,d,i,j,k;
    while(a!=-1)
    {
        scanf("%d",&a);
        if(a==-1)
        {
            break;
        }
        k=a*a;
        printf("%d",k);
        printf("
");
    }
}